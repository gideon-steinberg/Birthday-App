- @links = {"price" => "Current Stock Prices", "game" => "Game"}
%ul.breadcrumb
  - @links.each do |link, title|
    %li
      %a{:href => "/stocks/#{link}"}
        = title

%div{:style => "color: #000000;background-color: #00FF00"}
  %h1 
    Welcome to the site!
